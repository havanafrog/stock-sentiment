지금 auto/ui 브랜치의 별도 작업칸이다. 여기서 live.html 의 폰(390x844) 화면을 혼자 고친다.
앞선 판들은 main 에 합쳐졌고 이 작업칸도 그 위에 올라와 있다.

네가 물었던 것 둘 다 답이 왔다:
  가로 폰(844x390) — **안 한다.** 사람이 그냥 두기로 했다. 대기열에서 빼라. 다시 묻지 마라.
  느린 망 — **사람이 이미 고쳤다.** server.mjs 에 zlib 압축이 들어갔고 main 에 합쳐졌다.
    live.html 156KB -> 51KB, api/live 63KB -> 17KB, api/candles 22KB -> 8KB, 첫 로드 287KB -> 88KB.
    png 는 이미 눌려 있어 건너뛴다. 이것도 대기열에서 빼라.
    다만 네 시험 서버(8741)는 이 작업칸의 server.mjs 로 도는지 확인해 봐라 —
    옛 것이 떠 있으면 압축이 안 걸린 상태로 재게 된다.

대기열:
  1. 데스크톱 차트 '지금 값' 배지 글자를 폰과 같이 검게 한다. 사람이 승인했다 —
     읽히게 하는 수정은 둘 다 바로 고친다. 폰은 지난 판에 고쳤고(.cnow fill #0a0a0b,
     대비 3.71 -> 5.33 / 2.99 -> 6.61) 데스크톱만 흰 글자로 남아 3.71 / 2.99 다.
     그 한 줄을 640px 블록 밖으로 옮기면 된다. 옮긴 뒤 폰 값이 그대로인지도 다시 재라.
  2. 입력칸 placeholder 대비를 올린다. 세 입력칸(#tkBase 판 탭, #avgIn 실시간 탭,
     #pQ 글 탭)이 브라우저 기본 회색 rgb(117,117,117) 을 쓴다. 네가 잰 값은
     밝게 4.61, 시스템 어둡게 3.52, 손으로 어둡게 4.13 이다. 테마 변수로 색을 주고
     네 테마에서 4.5 이상인지 잰다. 데스크톱도 같이 고친다.
  3. contrast-test 가 SVG 글자(fill)와 placeholder 를 못 본다. 이번에 임시로 쟀던
     방식을 도구에 넣어 다음부터 저절로 잡히게 한다. 끝날 때 크롬 탭도 닫게 한다.
  4. 기존 시험 셋(phone-flow·overflow·theme-flip)이 PASS 뒤 종료 코드 127 로 죽는다.
     ws.close() 뒤 process.exitCode 로 끝내게 고친다. 종료 코드가 거짓이면 나중에
     진짜 실패를 못 잡는다.
  5. 다른 탭에서 번져 온 선언을 훑는다. dead-css 는 640px 블록 안만 본다.
     글 탭용 .pnote { margin: 10px 0 0 }(live.html:404)이 범위 없이 평단 줄에
     걸려 있던 것과 같은 종류다.

이번 판에 할 일:
1. 대기열에서 딱 하나만 골라 한다.
2. 크롬(127.0.0.1:9222)을 직접 붙여 재라. 눈대중으로 좋아졌다고 하지 마라.
   재기 전에 http://127.0.0.1:9222/json/list 의 page 를 닫고 시작해라.
3. 시험을 돌려 전부 통과하는지 확인해라. 주소는 http://127.0.0.1:8741/?k=R9HzHDGxVnjYeJ5Vqton1w
   node tools/chart-touch-test.mjs · phone-flow-test.mjs · contrast-test.mjs
   · overflow-test.mjs · theme-flip-test.mjs
4. 데스크톱(1280x900)도 같이 재서 안 망가졌는지 확인해라. 640px 규칙 밖으로 새면 안 된다.
5. 통과하면 한국어로 커밋해라. push 는 절대 하지 마라.
   커밋 메시지에 Co-Authored-By 나 Claude-Session 같은 꼬리줄을 붙이지 마라.

지킬 것:
- 한 판에 한 가지만. 검증 안 된 수정 위에 새 수정을 얹지 마라.
- 기능 추가, 데스크톱 재설계, 저장소 구조 변경은 하지 마라.
- **읽히게 하는 수정은 폰·데스크톱 둘 다 바로 고쳐라.** 물어볼 것 없다 —
  글자 대비, 누를 자리 크기, aria 속성, 읽기 도구에 읽히게 하는 것.
  같은 요소에 같은 결함이면 640px 블록 밖으로 옮겨 둘 다 고친다.
  여전히 폰 전용인 것: 배치, 접기, 크기, 순서 — 화면 폭에 따라 달라야 하는 것.
- 네가 앞서 잰 값도 의심해라. 이번 판에 × 단추가 그랬고, 대비 잣대도 두 번 고쳤다.
- 사람의 결정이 필요한 일은 답 맨 위에 "사람 필요" 와 물어볼 것을 적어라.
  다만 이미 답이 온 것(가로 폰, 느린 망)은 다시 묻지 마라.

대기열이 끝나면 폰 화면에서 아직 안 잰 곳을 스스로 찾아 대기열을 새로 적어라.
정말로 더 잴 것도 고칠 것도 없으면 아무것도 바꾸지 말고 '없음' 한 단어만 답해라.

답의 맨 끝에는 반드시 다음 줄들을 그대로 붙여라. 이 답이 다음 판의 물음이 된다:

---
다음 판에 할 일: (한 가지. 방금 잰 수치와 근거를 같이 적어라)
남은 대기열: (끝난 것은 빼고 다시 적어라)
이어서 지킬 것: 위 1~5 번 절차와 지킬 것을 그대로 반복한다. 시험 주소와 크롬 포트는 위와 같다.
---
