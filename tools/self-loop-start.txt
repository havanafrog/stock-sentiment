지금 auto/ui 브랜치의 별도 작업칸이다. 여기서 live.html 의 폰(390x844) 화면을 혼자 고친다.
앞선 판들은 main 에 합쳐졌고 이 작업칸도 그 위에 올라와 있다. 아래 수치는 합친 뒤 것이다.

재 놓은 것 (390x844, 종목 6개):
  실시간 탭 — 차트 위 395px · 봉 380px · 거래량 778~824px(첫 화면 안) · 문서 1485px
  글 탭   — 글 첫 줄 284px · 문서 8386px · 글 한 장 중앙값 86px
  대비    — --faint 는 밝은 화면 4.53~5.11, 어두운 화면 4.74~4.94 로 전부 AA 통과

대기열:
  1. 고른 탭 단추의 글자 대비를 올린다. 네가 앞 판에 범위 밖이라고 보고만 했던 것이고,
     사람이 고치라고 했다. 고른 탭(aria-selected="true")의 글자가 파랑 #64a8ff 이고
     바탕이 흰색이라 대비 2.22 다. 밝은 화면과 어두운 화면 둘 다 같다.
     지금까지 고친 어느 것보다 나쁘고, 탭은 늘 보이는 자리다.
     먼저 크롬으로 실제로 칠해진 색과 바탕을 걷어 확인해라 — 내가 준 값이 아니라
     네가 잰 값으로 고쳐라. 앞 판에도 내가 준 색이 두 번 틀렸다.
     목표는 AA 4.5 이상이되, 고른 탭과 안 고른 탭이 눈으로 구별돼야 한다
     (지금은 색으로만 갈린다 — 굵기나 밑줄이 이미 있는지 먼저 보고 정해라).
     파랑 색조 자체는 tossinvest 실측값이라 강조선·점 같은 다른 쓰임은 건드리지 마라.

이번 판에 할 일:
1. 대기열에서 딱 하나만 골라 고쳐라.
2. 크롬(127.0.0.1:9222)을 직접 붙여 고치기 전후 치수와 대비를 재라.
   눈대중으로 좋아졌다고 하지 마라.
3. 이 둘을 돌려 전부 통과하는지 확인해라:
   node tools/chart-touch-test.mjs http://127.0.0.1:8741/?k=R9HzHDGxVnjYeJ5Vqton1w
   node tools/phone-flow-test.mjs http://127.0.0.1:8741/?k=R9HzHDGxVnjYeJ5Vqton1w
4. 데스크톱(1280x900)도 같이 재서 안 망가졌는지 확인해라. 640px 규칙 밖으로 새면 안 된다.
5. 통과하면 한국어로 커밋해라. push 는 절대 하지 마라.
   커밋 메시지에 Co-Authored-By 나 Claude-Session 같은 꼬리줄을 붙이지 마라.
   저장소 주인 이름 하나로만 남긴다.

지킬 것:
- 한 판에 한 가지만. 검증 안 된 수정 위에 새 수정을 얹지 마라.
- 기능 추가, 데스크톱 재설계, 저장소 구조 변경은 하지 마라.
  폰에서 재서 나아지는 것만 한다.
- 시험이 깨졌는데 두 판 안에 못 고치겠으면, 고치려 들지 말고 답 맨 위에
  "사람 필요" 라고 적어라.
- 사람의 결정이 필요한 일(무엇을 먼저 보일지 같은)은 혼자 정하지 말고 답 맨 위에
  "사람 필요" 와 물어볼 것을 적어라.

대기열 1번이 끝나면, 폰 화면을 처음부터 다시 훑어 새로 잴 곳을 네가 찾아라.
여태 잰 곳은 위에 적힌 자리들이다. 아직 안 본 것으로는 가로로 눕힌 화면(844x390),
글자를 키운 설정(200% 확대), 느린 네트워크, 화면 읽어 주는 도구가 있다.
그중 폰에서 실제로 손해가 큰 것 하나를 골라 재고, 잰 값을 근거로 대기열을 새로 적어라.
정말로 더 잴 것도 고칠 것도 없으면 아무것도 바꾸지 말고 '없음' 한 단어만 답해라.

답의 맨 끝에는 반드시 다음 줄들을 그대로 붙여라. 이 답이 다음 판의 물음이 되니,
다음 판의 내가 앞뒤 사정 없이도 바로 일할 수 있어야 한다:

---
다음 판에 할 일: (한 가지. 방금 잰 수치와 근거를 같이 적어라)
남은 대기열: (끝난 것은 빼고 다시 적어라)
이어서 지킬 것: 위 1~5 번 절차와 지킬 것을 그대로 반복한다. 시험 주소와 크롬 포트는 위와 같다.
---
